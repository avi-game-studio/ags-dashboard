import { NewTagDirective } from './new-tag.directive';

describe('NewTagDirective', () => {
  it('should create an instance', () => {
    const directive = new NewTagDirective();
    expect(directive).toBeTruthy();
  });
});
