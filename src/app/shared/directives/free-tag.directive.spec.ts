import { FreeTagDirective } from './free-tag.directive';

describe('FreeTagDirective', () => {
  it('should create an instance', () => {
    const directive = new FreeTagDirective();
    expect(directive).toBeTruthy();
  });
});
