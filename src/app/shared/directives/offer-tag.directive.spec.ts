import { OfferTagDirective } from './offer-tag.directive';

describe('OfferTagDirective', () => {
  it('should create an instance', () => {
    const directive = new OfferTagDirective();
    expect(directive).toBeTruthy();
  });
});
